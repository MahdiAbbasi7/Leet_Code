class Solution:
    def checkIfExist(self, arr: list[int]) -> bool:
        # for i in range(len(arr)):
        #     hashTable = list(arr[0, i-1])
        # print(hashTable)
        for i in range(len(arr)):
            #todo: complete this line.
            pass

test = Solution()
test.checkIfExist([10,2,5,3])